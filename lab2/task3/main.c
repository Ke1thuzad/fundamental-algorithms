#include "main.h"

int main(int argc, char** argv) {
    char a[] = "lilila", b[] = "mililiters of lililoo are great for lililaloo";
//    printf("%c\n", b[36]);
    printf("%d\n", substr(b, len(b), a, len(a)));

//    int n = 10, table[n];
//    shift_table(a, n, table);
//    for (int i = 0; i < n; ++i) {
//        printf("%d ", table[i]);
//    }

    return 0;
}